import { Expense } from '@/util/AppTypes'
import React from 'react'

const ExpenseResponsiveColumn = ({ expenses }: { expenses: Expense[] }) => {
  return (
    <div>ExpenseResponsiveColumn</div>
  )
}

export default ExpenseResponsiveColumn
