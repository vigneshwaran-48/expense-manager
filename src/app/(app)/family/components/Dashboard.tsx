import { Family } from '@/util/AppTypes';
import React from 'react'

const Dashboard = ({ family }: { family: Family }) => {
    
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  )
}

export default Dashboard;
